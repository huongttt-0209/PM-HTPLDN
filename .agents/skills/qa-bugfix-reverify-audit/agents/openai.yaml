interface:
  display_name: "QA Bugfix Reverify Audit"
  short_description: "Re-test dev-fixed bugs and unblocked TC"
  default_prompt: "Use $qa-bugfix-reverify-audit to re-verify bugs dev claims fixed, update bug verdicts, and list TC/path now unblocked."

policy:
  allow_implicit_invocation: true
